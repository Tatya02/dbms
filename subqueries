USE prec;
CREATE TABLE employee (
 emp_id INT PRIMARY KEY,
 name VARCHAR(50),
 salary INT,
 dept VARCHAR(30)
);

INSERT INTO employee VALUES
(1,'Rahul',25000,'HR'),
(2,'Sneha',30000,'IT'),
(3,'Amit',28000,'Sales');

SELECT name, salary
FROM employee
WHERE salary = (
    SELECT MAX(salary) 
    FROM employee
);

SELECT name
FROM employee
WHERE salary > (
   subqueries SELECT AVG(salary) 
    FROM employee
);
