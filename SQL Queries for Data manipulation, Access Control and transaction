USE prec;
CREATE TABLE employee (
 emp_id INT PRIMARY KEY,
 name VARCHAR(50),
 salary INT,
 dept VARCHAR(30)
);

INSERT INTO employee VALUES
(1,'Rahul',25000,'HR'),
(2,'Sneha',30000,'IT'),
(3,'Amit',28000,'Sales');

UPDATE employee
SET salary = salary + 5000
WHERE emp_id = 1;

DELETE FROM employee
WHERE emp_id = 3;

SELECT * FROM employee WHERE salary > 25000;
SELECT * FROM employee WHERE dept='IT' AND salary>=30000;
SELECT * FROM employee WHERE name LIKE 'R%';
SELECT UPPER(name) FROM employee;

GRANT SELECT, INSERT ON college.employee TO 'user1'@'localhost';

REVOKE INSERT ON college.employee FROM 'user1'@'localhost';

START TRANSACTION;

INSERT INTO employee VALUES(4,'Kiran',26000,'Admin');

SAVEPOINT sp1;

UPDATE employee SET salary=35000 WHERE emp_id=2;

ROLLBACK TO sp1;

COMMIT;
